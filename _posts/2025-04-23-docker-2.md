---
layout: single
title: ¿Cómo instalamos Docker en Linux? 
date: 2025-04-18
classes: wide
header:
  teaser: /assets/images/slae32.png
categories:
  - Docker
tags:
---

Para instalar Docker en Linux se debe hacer por terminal, usando este comando:

```bash
sudo apt install docker.io
```

Cuando tengamos instalado Docker, hay que activar el servicio porque puede ser que no venga activo por defecto, para activarlo hacemos lo siguiente:

![](/assets/images/docker/2.png)

Con este comando ejecutado, nos aseguramos de que Docker estará activo.